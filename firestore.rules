rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /researchhq/main {
      // Anyone can read (public visibility)
      allow read: if true;
      // Allow guests to write so they can add tasks
      allow write: if true;
    }
  }
}
